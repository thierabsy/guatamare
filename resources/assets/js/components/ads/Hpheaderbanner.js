import React from 'react';
import { urlPath } from '../path';

const Hpheaderbanner = () => {
        return (
            <div className="hpheaderbanner">
                <img src={`${urlPath}/img/banner4.jpg`} />
            </div>
        );
    }

export default Hpheaderbanner;
